# คัดลอกไฟล์นี้เป็น .env แล้วแก้ค่าตามต้องการ
# cp .env.example .env

# URL เชื่อมต่อ PostgreSQL (ต้องตรงกับ docker-compose.yml)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fullstack_db?schema=public"

# Port ที่ Backend รัน
PORT=3001
